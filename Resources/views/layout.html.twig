
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <style>
        body { background: #F5F5F5; font: 18px/1.5 sans-serif; }
        h1, h2 { line-height: 1.2; margin: 0 0 .5em; }
        h1 { font-size: 36px; }
        h2 { font-size: 21px; margin-bottom: 1em; }
        p { margin: 0 0 1em 0; }
        a { color: #0000F0; }
        a:hover { text-decoration: none; }
        code { background: #F5F5F5; max-width: 100px; padding: 2px 6px; word-wrap: break-word; }
        #wrapper { background: #FFF; margin: 1em auto; max-width: 800px; width: 95%; }
        #container { padding: 2em; }
        #welcome h1 span { display: block; font-size: 75%; }
        #comment p { margin-bottom: 0; }
        .col { width: 30%; display: block; float: left; margin: 5px; padding: 10px 5px; background: rgba(91,192,222,0.3); min-height: 125px;}
        .clear {clear: both;}
        h3 {margin: 0;}
        input {margin-right: 20px;}
        .doclink {display: block;padding-left: 40px;}
        .composername {display: block;padding-left: 40px;}
        input[type="submit"] {
            margin: 20px auto 0 auto;
            display: block;
            font-size: 15px;
            background: #5cb85c;
            color: white;
            border-radius: 3px;
            padding: 10px;
        }
        .installed {color: #5cb85c; margin: 10px 0 0 0; padding-left: 40px; font-size: 15px;}
        .installing {color: #f0ad4e;  margin: 10px 0 0 0; padding-left: 40px; font-size: 15px;}
        .ready {color: #5bc0de;  margin: 10px 0 0 0; padding-left: 40px; font-size: 15px;}

        @media (min-width: 768px) {
            #wrapper { width: 80%; margin: 2em auto; }
            #status a, #next a { display: block; }

            @-webkit-keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
            @keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
        }
    </style>
    <title>Welcome to FitzBundle !</title>
</head>
<body>
<div id="wrapper">
    <div id="container">
        {% block body %}{% endblock %}
    </div>
    {#<div id="comment">#}
        {#<p>#}
            {#You're seeing this page because debug mode is enabled and you haven't configured any homepage URL.#}
        {#</p>#}
    {#</div>#}
</div>
</body>
</html>
