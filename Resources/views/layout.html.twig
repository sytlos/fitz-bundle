
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <style>
            body { background: #F5F5F5; font: 18px/1.5 sans-serif; }
            h1, h2 { line-height: 1.2; margin: 0 0 .5em; }
            h1 { font-size: 36px; }
            h2 { font-size: 21px; margin-bottom: 1em; }
            p { margin: 0 0 1em 0; }
            a { color: #0000F0; }
            a:hover { text-decoration: none; }
            code { background: #F5F5F5; max-width: 100px; padding: 2px 6px; word-wrap: break-word; }
            #wrapper { background: #FFF; margin: 1em auto; max-width: 800px; width: 95%; }
            #container { padding: 1em; border-top: 5px solid #5bc0de;}
            #welcome h1 span { display: block; font-size: 75%; }
            #comment p { margin-bottom: 0; }
            .col { width: 30%; display: block; float: left; margin: 5px; padding: 10px 5px; background: rgba(91,192,222,0.2); min-height: 100px; border-left: 2px solid #5bc0de;}
            h3 {margin: 0;}
            .clear {clear: both;}
            .doclink {display: block; padding-left: 20px; font-size: 13px;}
            .composername {display: block; padding-left: 20px; font-size: 13px;}
            input[type="submit"] {
                font-size: 15px;
                background: #5bc0de;
                color: white;
                border-radius: 3px;
                padding: 5px 15px;
                outline: none;
            }
            .installed {color: #5cb85c; margin: 10px 0 0 0; padding-left: 20px; font-size: 13px;}
            .installing {color: #f0ad4e;  margin: 10px 0 0 0; padding-left: 20px; font-size: 13px;}
            .ready {color: #5bc0de;  margin: 10px 0 0 0; padding-left: 20px; font-size: 13px;}
            label {font-size: 13px; margin-bottom: 10px; display: block;}
            #searchform {float: left; width: 49%; margin-left: 5px;}
            #submitform {float: left; width: 49%; text-align: right; margin-left: -5px; margin-bottom: 15px;}
            #search {padding: 5px; outline: none; width: 200px;}
            .hide {display: none;}

            @media (min-width: 768px) {
                #wrapper { width: 80%; margin: 2em auto; }
                #status a, #next a { display: block; }

                @-webkit-keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
                @keyframes fade-in { 0% { opacity: 0; } 100% { opacity: 1; } }
            }
        </style>
        <title>Welcome to FitzBundle !</title>
    </head>
    <body>
        <div id="wrapper">
            <div id="container">
                {% block body %}{% endblock %}
            </div>
        </div>
    </body>
    <script>
        var search = document.getElementById('search');
        var bundles = document.querySelectorAll('.col');

        search.addEventListener('input', function () {
            bundles.forEach(function (bundle) {
                var composer = bundle.querySelector('.composername').textContent;
                var name = bundle.querySelector('label').textContent;

                if (!composer.includes(search.value) && !name.includes(search.value)) {
                    bundle.classList.add('hide');
                } else {
                    bundle.classList.remove('hide');
                }
            });
        });
    </script>
</html>
